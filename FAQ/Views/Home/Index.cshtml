@model IEnumerable<FAQ.Models.Categories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

   @using (Html.BeginForm("Index", "Home"))
   {
    @Html.DropDownList("Categories", ViewBag.ListItems as SelectList, new { onchange = "submit();" })
   }
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.CategoryName, "FAQList", "FAQuestion", new { id = item.CategoryID },null)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryID })
        </td>*@
    </tr>
}

</table>
